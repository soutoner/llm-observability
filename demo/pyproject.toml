[project]
name = "llm-observability-demo"
version = "0.1.0"
requires-python = ">=3.10"
dependencies = [
    "langfuse>=2.0.0",
    "openai>=1.0.0",
    "python-dotenv>=1.0.0",
]

[dependency-groups]
dev = ["ruff>=0.0.0"]

[tool.ruff]
line-length = 100
